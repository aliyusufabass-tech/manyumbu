export function validateTextMessage(text: string) { const trimmed = text.replace(/\x00/g, "").trim(); if (!trimmed) return "Message text is required."; if (trimmed.length > 5000) return "Message text must be 5000 characters or fewer."; return null; }
export function validateVoiceNote(durationSeconds: number, sizeBytes: number, mimeType: string) { if (!mimeType.startsWith("audio/")) return "Voice notes must be audio files."; if (durationSeconds <= 0) return "Voice note duration is required."; if (durationSeconds > 15 * 60) return "Voice notes must be 15 minutes or shorter."; if (sizeBytes > 25 * 1024 * 1024) return "Voice notes must be 25MB or smaller."; return null; }
export function validateDocument(name: string, sizeBytes: number, mimeType: string) { const blocked = /\.(exe|bat|cmd|com|scr|js|vbs|ps1|sh|dll|msi)$/i; if (blocked.test(name)) return "This document type is not allowed."; const allowed = ["application/pdf", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "application/vnd.openxmlformats-officedocument.presentationml.presentation", "text/plain"]; if (!allowed.includes(mimeType)) return "Unsupported document format."; if (sizeBytes > 25 * 1024 * 1024) return "Documents must be 25MB or smaller."; return null; }
